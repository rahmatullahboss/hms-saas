---
name: typescript
description: "Typescript for ozzyl-hms. 3 gotchas, 2 fixes."
domain: typescript
triggers:
  - glob: "**/*.ts"
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript

Auto-compiled from **10 real patterns** in **ozzyl-hms**. This skill is auto-routed to agents when working on typescript files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| BUG: FIX: Was querying m.expiry_date which doesn't | Found in src/routes/tenant/reportPharmacy.ts |
| BUG: FIX: Was referencing m.unit_price and m.quant | Found in src/routes/tenant/reportPharmacy.ts |
| BUG: FIX: Was referencing non-existent columns. No | Found in src/routes/tenant/reportPharmacy.ts |

## 🔧 Problem Playbooks

### Patched security issue HTTPException — prevents XSS injection attacks
- /**
+  */
-  * POST /api/shareholders/ocr-pdf
+ shareholderRoutes.post('/ocr-pdf', async (c) => {
-  * Accepts multipart/form-data with a PDF file (max 10MB).
+   if (!c.env.GEMINI_API_KEY) {
-  * Sends it to Gemini Flash as base64 and returns structured shareholder JSON.
+     throw new HTTPException(503, {
-  */
+       message: 'OCR service not configured. Contact your administrator to set GE

**Actionable Steps:**
1. Modified 1 files
2. identifier: HTTPException
3. identifier: OCR
4. identifier: Contact
5. identifier: Unauthorized

### Patched security issue Cloudflare — prevents XSS injection attacks
-   if (!file || !(file instanceof File)) {
+   // Cloudflare Workers FormData returns Blob (with .name property)
-     throw new HTTPException(400, { message: 'file field is required' });
+   if (!file || typeof file !== 'object' || !('size' in file)) {
-   }
+     throw new HTTPException(400, { message: 'file field is required' });
- 
+   }
-   const MAX_SIZE = 10 * 1024 * 1024;
+   const upload

**Actionable Steps:**
1. Modified 1 files
2. identifier: Cloudflare
3. identifier: Workers
4. identifier: FormData
5. identifier: Blob

---
*Auto-generated by BrainSync 🧠 | 10 patterns | 2026-03-22*
