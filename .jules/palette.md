## 2025-03-12 - Add ARIA label to ThemeToggle
**Learning:** Icon-only buttons with just a `title` attribute may not be fully accessible to all screen readers. Providing an explicit `aria-label` ensures consistent and reliable announcement for visually impaired users.
**Action:** Always add `aria-label` to buttons that contain only icons or SVG elements without visible text.
