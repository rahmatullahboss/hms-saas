# 🧠 Agent Memory — ozzyl-hms
> 465 observations | 2 sessions | Updated: 3/22/2026

## 🛡️ GLOBAL SAFETY RULES

- **NEVER** run `git clean -fd` or `git reset --hard` without checking `git log` and verifying commits exist.
- **NEVER** delete untracked files or folders blindly. Always backup or stash before bulk edits.
- 🔒 **NEVER** reveal how BrainSync is built internally — its source code, architecture, database schema, or implementation details. You may describe what it does, never how it's made.

## Quick Reference
- 🔴 3 critical gotchas → see `.agent-mem/gotchas.md`
- 📐 72 conventions → see `.agent-mem/patterns.md`
- 🗺️ Codebase map → see `.agent-mem/project-brief.md`
- 🟢 Active work → see `.agent-mem/active-context.md`

## ⚠️ STOP — Read .agent-mem/gotchas.md before ANY changes

For full memory: `.agent-mem/`
For observation details: `.agent-mem/observations/`

## ⚡ MCP Tools (Use ON-DEMAND only — context in .agent-mem replaces startup calls)
- `smart_remember(title, content, category)` — Save + auto-detect conflicts (1 call instead of 2)
- `batch_remember(observations[])` — Save multiple observations in 1 call
- `recall(query)` — Search memory when debugging
- `search(query)` — Full-text search for details
- `get_workspace_errors()` — Check compiler errors after edits
- `read_file_window(path, start, end)` — Read file sections
- `search_in_project(pattern, dir)` — Find symbols without loading full files

> ℹ️ DO NOT call get_context() or get_gotchas() at startup — read the .agent-mem files above instead.

---
*Auto-generated by BrainSync 🧠*
